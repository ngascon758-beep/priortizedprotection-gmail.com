[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ppm-core"
version = "0.1.0"
description = "Autonomic Execution Gate for Autonomous AI Agents"
readme = "README.md"
authors = [{ name = "Alex Gascon" }]
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0.0"]
